node_modules
.tmp
.cache
dist
build
.env
.env.*
!.env.example
.git
npm-debug.log
# Uploaded media lives on a persistent bind mount/volume in docker-compose,
# never baked into the image (would bloat it and go stale on rebuild).
public/uploads
